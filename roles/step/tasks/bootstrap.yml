---
- name: Bootstrap CA
  command: 
    argv:
      - step ca bootstrap 
      - --ca-url {{ step_ca_url|quote }} 
      - --ca-fingerprint {{ step_ca_fingerprint|quote }}
      - --install
  become: yes
  environment:
    STEPPATH: /etc/step